[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "a-share-quant-codex"
version = "0.1.0"
description = "A-share quant research prototype built for Codex collaboration"
requires-python = ">=3.10"
