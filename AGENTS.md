# AGENTS.md

## 项目目标

这是一个面向 A 股的量化研究、回测、监控与推送仓库。
Codex 在本仓库中的任务只有一件事：

| 目标 | 说明 |
| --- | --- |
| 维护研究流程一致性 | 保证数据、特征、策略、回测、评估、推送口径一致 |
| 新增因子 / 模型 / 风控模块 | 必须能解释、能复现、能验证 |
| 自动补测试与文档 | 改完就补，不允许只改代码不留痕 |
| 审计未来函数、交易约束与成本模型 | 任何收益都必须先过这一关 |

## 核心原则

| 规则 | 说明 |
| --- | --- |
| 第一性原理 | 从原始需求出发。动机不清立刻停，路径非最优直接纠正。 |
| 极简沟通 | 用简单直白的中文一次性输出，把用户当高中生。拒绝角色扮演，拒绝无意义分段，已经解决的问题后续绝不再提。不要使用 P0/P1/P2 术语。 |
| Let it crash | 发现问题尽早暴露。严禁用降级、兜底、后发式补丁或含糊的通用后处理掩盖问题。 |
| 禁止擅自开分支 | 严禁私自创建新 worktree。可以提建议，但必须得到用户明确同意后再操作。 |
| 自检与精简 | 每次改动后，必须先 Review 查 Bug，再按第一性原理重想一遍：有没有更简单、更稳健的做法。 |

## 开发工作流

| 层级 | 规则 |
| --- | --- |
| 分析层 | 文字、图标、颜色等轻量 UI 修改，直接执行并落地归档。 |
| 规划层 | 重大重构、多任务并行、跨模块改造，先做流程编排并更新全局流程图。 |
| 任务层 | 复杂任务维护 `task_plan.md`、`progress.md`、`findings.md`。 |
| 执行层 | 严格遵守 `propose -> 用户确认 -> apply -> archive`。 |
| 粒度控制 | 动手前先拆成 `<files>/<action>/<verify>/<done>`。 |

## 工程规范

| 规则 | 说明 |
| --- | --- |
| 不可捏造数据 | 生产代码严禁 Mock。Mock 只允许本地调试，统一走 `127.0.0.1:xxxx/mock`，并必须加入 `.gitignore`。 |
| 自动化执行 | `curl`、`cat`、`git` 等命令可直接执行；Playwright 持续会话禁止无意义暂停。 |
| 子代理分流 | 多于 1 个子问题、需要并行分析、Review 或研究时，必须拆分并使用子代理，保持主上下文干净。 |
| API 接入 | 优先参考 `/Users/ifengz/CodingCase/002_DevelopSpec/1.Doc/` 的现成方案，并把新方案持续补充回去。 |
| 自我进化 | 用户指正后，立即更新 `lessons.md`。开始新任务前必须先回顾 `lessons.md`。 |

## 运维安全守则

| 规则 | 说明 |
| --- | --- |
| 排障顺序 | 遇到网络、证书、代理异常，优先查入口和反代配置。严禁仅凭临时 `IP:端口` 访问失败就判定数据库或后端损坏。必须先确认固定入口，如域名或面板地址。 |

## 输出规范

| 规则 | 说明 |
| --- | --- |
| 禁止陈述式汇报 | 不要复读背景，不要把简单问题拆成“证据/分析/结论”长篇汇报。 |
| 结论先行 | 直接给结论和修补方案。解释必须短、硬、白话。 |
| 表格化输出 | 评审、对比、多项任务、方案选择，优先 Markdown 表格。 |
| 强制收口 | 每次结束必须明确说明这次用到的 skill。 |

## 项目专属硬约束

| 规则 | 说明 |
| --- | --- |
| 面向 A 股 | 不要默认套用美股 T+0 逻辑。 |
| 新策略说明义务 | 任何新策略都必须写清：信号频率、调仓频率、未来函数风险、是否考虑停牌 / ST / 涨跌停 / 成本 / 换手。 |
| 新因子最低要求 | 任何新增因子都应支持截面标准化，至少一种中性化方式（size / industry），并补单测或最小回归测试。 |
| 回测可运行 | 任何改动都应尽量保持 `examples/run_mock_backtest.py` 可运行。 |
| 数值真值来源 | 不允许把 LLM 当作精确数值真值来源。所有绩效指标必须由 Python 代码计算。 |

## 工作顺序

每次开始任务前优先读：

| 顺序 | 文件 |
| --- | --- |
| 1 | `README.md` |
| 2 | `docs/study_guide.md` |
| 3 | `docs/architecture.md` |
| 4 | `lessons.md` |
| 5 | 如果任务匹配，读对应 skill 的 `SKILL.md` |

## 验证命令

优先运行：

```bash
.venv/bin/python examples/run_mock_backtest.py
.venv/bin/python -m unittest discover -s tests
```

## 代码规范

| 规则 | 说明 |
| --- | --- |
| Python 版本 | Python 3.10+ |
| 类型注解 | 默认必须写 |
| Docstring | 关键函数必须写 |
| 函数长度 | 尽量拆成 20-60 行的小函数 |
| 注释 | 只写必要信息，不解释显然代码 |

## 任务模板

### 新增 / 修改因子

| 步骤 | 要求 |
| --- | --- |
| 1 | 明确因子定义与经济含义 |
| 2 | 明确属于技术面 / 基本面 / 微观结构 / 另类数据哪一类 |
| 3 | 明确标签 horizon |
| 4 | 实现因子 |
| 5 | 做标准化 / 中性化 |
| 6 | 回测或至少跑 IC 检查 |
| 7 | 写结果摘要 |

### 增强回测

| 步骤 | 要求 |
| --- | --- |
| 1 | 先识别当前回测的近似假设 |
| 2 | 再补交易约束或成本逻辑 |
| 3 | 追加测试 |
| 4 | 在 `docs/architecture.md` 更新假设 |
